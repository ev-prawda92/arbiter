# Runtime dependencies, pinned to the versions validated by `make check`
# (all gates + the full release gate). Bump deliberately, then re-run `make check`.
fastapi==0.141.1
uvicorn==0.46.0
httpx==0.28.1
cryptography==46.0.7
PyJWT[crypto]==2.12.1
